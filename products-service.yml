spring:
  datasource:
    url: jdbc:mysql://localhost:3306/products_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: products_user
    password: products_password
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8082